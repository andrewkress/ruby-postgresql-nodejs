sudo: required

language: ruby

branches:
  only:
  - master

services:
  - docker

install:
  - echo "install nothing!"

script:
  - docker --version
  - docker build -t ruby-postgresql-nodejs .

deploy:
  provider: script
  script: echo "deploy to dockerhub if on master"
  on:
    branch: master
